<?
## 디렉토리 정의.
$upload_dir			= $HOME_DIR."/zb/upload";					// 게시판 업로드 인터넷 경로($HOME_DIR 은 common.inc 참조)
$upload_root		= $ABS_DIR."/zb/upload";					// 게시판 업로드 서버 경로($ABS_DIR 은 common.inc 참조)

## 새글에 "new" 를 표시할 최근날짜
$new_date = 1;
?>